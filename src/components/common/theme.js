const { createTheme } = require("@mui/material/styles");

const theme = createTheme({
    typography: {
        fontFamily: `'Nanum Gothic', 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif`
    }
});

export default theme;
